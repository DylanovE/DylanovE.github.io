// eslint-disable-next-line no-undef
export const publicPath = process.env.NODE_ENV === "production" ? "/REPO_NAME/" : "/";

// used to be this but module gave a error
// module.exports = {  publicPath: process.env.NODE_ENV === "production" ? "/REPO_NAME/" : "/",};
